server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/workspace/learning/kotlin/user-management-evolution-kotlin
          searchPaths: configuration
